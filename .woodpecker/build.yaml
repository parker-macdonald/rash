when:
  - event: push

steps:
  - lint:
    image: debian
    commands:
      - apt-get update
      - apt-get upgrade
      - apt-get install make clang-tidy
      - make check

  - name: build
    image: silkeh/clang
    commands:
      - make